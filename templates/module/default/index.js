import reducerRegistry from '../../reducerRegistry';

// CONSTS

const initialState = {};

// ACTIONS

// REDUCER
const classNameReducer = (state = initialState, action) => {};

reducerRegistry.register('className', classNameReducer);