import { connect } from 'react-redux';
import classNameComponent from '../components/classNameComponent';

const mapDispatchToProps = dispatch => ({});

const mapStateToProps = state => ({});

export default connect(mapStateToProps, mapDispatchToProps)(classNameComponent);